{{- if .Values.controlPlane.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ include "featbit.fullname" . }}-controlplane
  labels:
    {{- include "featbit-metadata-labels-common" . | nindent 4 }}
    app.kubernetes.io/component: controlplane
  annotations:
    {{- include "featbit-metadata-annotations-common" . | nindent 4 }}
spec:
  {{- if not .Values.controlPlane.autoscaling.enabled }}
  replicas: {{ .Values.controlPlane.replicaCount }}
  {{- end }}

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: {{ .Values.controlPlane.rollout.maxSurge }}
      maxUnavailable: {{ .Values.controlPlane.rollout.maxUnavailable }}

  selector:
    matchLabels:
      {{- include "featbit.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: controlplane
  template:
    metadata:
      {{- with .Values.controlPlane.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "featbit.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: controlplane
        {{- with .Values.controlPlane.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- include "featbit.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "featbit.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.controlPlane.podSecurityContext | nindent 8 }}
      initContainers:
        {{- include "initContainers-wait-for-infrastructure-dependencies" (dict "context" . "component" "controlplane") | indent 8 }}
      containers:
        - name: {{ .Chart.Name }}-controlplane
          securityContext:
            {{- toYaml .Values.controlPlane.securityContext | nindent 12 }}
          image: {{ include "featbit.controlPlane.image" . }}
          imagePullPolicy: {{ .Values.controlPlane.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 5200
              protocol: TCP
          livenessProbe:
            periodSeconds: 5
            timeoutSeconds: 2
            httpGet:
              path: /health/liveness
              port: http
          readinessProbe:
            periodSeconds: 10
            timeoutSeconds: 5
            httpGet:
              path: /health/readiness
              port: http
          resources:
            {{- toYaml .Values.controlPlane.resources | nindent 12 }}
          env:
            {{- include "postgresql-env" . | indent 12 }}
            {{- include "redis-env" . | indent 12 }}
            {{- include "mongodb-env" . | indent 12 }}
            {{- include "otel-common-env" . | indent 12 }}
            {{- include "controlplane-otel-env" . | indent 12 }}
            {{- include "kafka-bootstrapservers" . | indent 12 }}
            {{- with .Values.controlPlane.env }}
            {{ toYaml . | nindent 12 }}
            {{- end }}
      {{- with .Values.controlPlane.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.controlPlane.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.controlPlane.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}